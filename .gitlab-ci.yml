# Specify the image to use for the job
image: node:14

stages:
  - build
  - test

job_build:
  stage: build
  script:
    - cd /home/website/kiraka
    - npm install
    - npm run build
    - echo "Building the project"

job_test:
  stage: test
  script:
    - echo "Running tests"
