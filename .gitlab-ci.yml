# Specify the image to use for the job
image: node:14
image: python

stages:
  - build
  - test
  - deploy

before_script:
  - cd /home/website/kiraka
  - source venv/bin/activate
  - pip install -r requirements.txt

job_build:
  stage: build
  script:
    - cd /home/website
    - ./run_app.sh

  only:
    - dev
    - master

job_test:
  stage: test
  script:
    - echo "Running tests"
  only:
    - dev
    - master

job_deploy:
  stage: deploy
  script:
    - echo "Deploying the project"
  only:
    - master
